apiVersion: "v1"
kind: "Secret"
type: "Opaque"

metadata:
  name: "ssh-to-prom-basicauth"
  namespace: "{{ $.Release.Namespace }}"

stringData:
  users: |
    {{ htpasswd $.Values.IngressBasicAuthUsername $.Values.IngressBasicAuthPassword }}

