apiVersion: "v1"
kind: "Service"

metadata:
  name: "ssh-to-prom"
  namespace: "{{ $.Release.Namespace }}"
  labels:
    app: "ssh-to-prom"

spec:

  selector:
    app: "ssh-to-prom"
  type: "ClusterIP"
  ports:
    - port: 80

